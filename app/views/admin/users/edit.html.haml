%div{:class => 'span-22 prepend-2 section_left last'}
  %h2=t('.title')
  = simple_form_for(@user, :url => admin_user_path(:id => @user.id), :html => {:multipart => true }) do |f|
    = render :partial => 'form', :locals => {:f => f}
